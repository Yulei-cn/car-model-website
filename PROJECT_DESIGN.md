# 车模网站项目设计说明

## 项目目标

这个项目是一个面向私域客户的静态车模展示网站。核心目标不是低价获客，而是让客户看到真实库存图片、法国本地代购定位、集运时间安排和可信联系方式。

主要客源预期来自小红书、闲鱼等平台。平台用于第一次触达，网站用于承接更完整的图片清单和集运说明；已成交客户通过包裹内明信片获得微信二维码，后续可以更直接沟通。

## 技术路线

- 第一阶段使用静态网站：HTML、CSS、JavaScript、GitHub/Vercel。
- 当前页面展示不需要 Supabase；后续如果要管理库存状态、价格、预约和客户询价，再接 Supabase。
- 车模数据由本地图片目录生成：`C:\Users\57799\Pictures\car\archive`。
- 更新库存时运行 `scripts/sync-assets.ps1`，自动复制图片并生成 `data/catalog.json`。
- 首页使用 WebP 缩略图，用户点开图库时才加载原图，降低 Vercel 出站流量。
- 本地打印文件通过 `.vercelignore` 排除在线部署，只保留在 GitHub 和本地仓库中。
- 通过 `vercel.json` 添加安全响应头和缓存策略；具体说明见 `SECURITY.md`。

## 隐私与联系方式策略

- 公网页面只放邮箱和联系说明。
- 微信二维码、收款码不放公开网页，避免被爬取、骚扰、盗图误用或被平台风控关联。
- 微信二维码、收款码只放在随包裹明信片中，因为收到包裹的人已经是成交客户，信任程度更高。

## 阶段流程

顺序固定为：网页 -> 明信片 -> 海报。

每完成一个阶段就提交并同步到 GitHub。明信片阶段完成后自动进入海报阶段，不再等待额外指令。

### 1. 网页

状态：已完成第一版并同步。

内容：
- 移动端优先首页。
- 现有模型照片目录。
- 搜索功能。
- 集运进度说明。
- 邮箱与隐私联系策略。

移动端要求：
- 手机访问是主场景，占比按 80%-90% 预估。
- 布局先按单列手机设计，再扩展到平板和电脑。
- 图片懒加载，卡片尺寸稳定，避免手机端横向滚动。

线上地址：
- Vercel：`https://car-model-website-smoky.vercel.app/`
- GitHub Pages：`https://yulei-cn.github.io/car-model-website/`

### 2. 明信片

状态：旧版 HTML/PNG 打印稿已删除，改为使用 `ai/` 中的 AI 生成图继续设计。

用途：
- 放入包裹内。
- 可以直接放微信二维码和收款码。
- 强化“法国代购”定位，提高复购和私域联系。

设计要求：
- 必须明显突出法国代购。
- 风格可以浪漫一点，允许铁塔、卢浮宫、法国邮戳、法式街景等元素。
- 5 个版本要明显不同，方便挑选。
- 第二版提示词写在 `AI生成关键词2.txt`。
- 当前 `ai/` 中已有明信片 1、2、3、5，缺少明信片 4。
- 已基于现有 AI 图继续加工输出到 `成品/明信片/1`、`成品/明信片/2`、`成品/明信片/3`、`成品/明信片/5`。
- 每个已加工版本包含 `正面.png` 和 `反面.png`，反面只放微信二维码占位。

### 3. 海报

状态：旧版 HTML/PNG 打印稿已删除，改为使用 `ai/` 中的 AI 生成图继续设计。

用途：
- 当前阶段可用 A4 纸打印。
- 后续可以交给工厂按正式尺寸、出血、纸张工艺重新导出。

设计要求：
- 与明信片一样提供 5 个版本。
- 突出法国本地车模代购、中高端收藏、集运节奏和真实库存照片。
- 当前 `ai/` 中已有 A4 海报 1、2、3、5，缺少 A4 海报 4。
- A6 小海报第二版提示词写在 `AI生成关键词2.txt`。
- 已基于现有 AI 图继续加工输出到 `成品/海报A4` 和 `成品/海报A6`。
- A4/A6 海报都已嵌入网站二维码。

当前版本方向：
- Paris night：铁塔轮廓加深色主视觉，强调法国代购。
- Louvre collection：卢浮宫玻璃金字塔氛围，强调收藏感。
- Group shipping：集运时间表海报，适合活动期使用。
- Performance：欧系性能车视觉，适合小红书或闲鱼引流。
- Merci：感谢与复购引导，适合随包裹夹页。

## 同步规范

每完成一个阶段，应执行：

```powershell
git add .
git commit -m "Complete postcard phase"
git push
```

当前 GitHub 远端仓库为 `https://github.com/Yulei-cn/car-model-website`。
